#include <iostream>
#include "Queue.h"

using namespace std;

// the Move struct holds the source (tower being moved from) and dest (tower being moved to) as ints.
// the move number will be tracked simply by the position of a Move object in the moves queue
struct Move {
	int source;
	int dest;
};

int main(){


	return 0;
}